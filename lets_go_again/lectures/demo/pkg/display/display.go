package display

import "fmt"

func Display(msg string) { // Use capital letters in functions so they can be seen by main package
	fmt.Println(msg)
}

// func hello(msg string) { // cannot be seen in main
// 	fmt.Println(msg)
// }
